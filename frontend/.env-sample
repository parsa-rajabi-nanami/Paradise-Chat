# API and WebSocket traffic uses the Vite dev proxy during local development.
# For a production build behind the supplied gateway, keep these same-origin.
VITE_API_URL=/api
VITE_WS_HOST=
VITE_DEV_BACKEND_URL=http://127.0.0.1:8000
VITE_SITE_URL=http://localhost:3000
