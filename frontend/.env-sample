# These values are embedded into the Vite build. VITE_API_URL includes /api;
# VITE_WS_HOST contains the host only.
VITE_API_URL=http://127.0.0.1:8000/api
VITE_WS_HOST=localhost:8000
VITE_SITE_URL=http://localhost:3000
